import {ProjectFilterByTooltipPipe} from './project-filter-by-tooltip.pipe';

describe('ProjectFilterByTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectFilterByTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
