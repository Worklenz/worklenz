import {LazyForDirective} from './lazy-for.directive';

describe('LazyForDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyForDirective();
    expect(directive).toBeTruthy();
  });
});
